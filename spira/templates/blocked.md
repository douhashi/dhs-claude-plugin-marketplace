# テンプレート: 人手対応待ち

- **書き手**: autopilot のライン（`spira:do` の実行中に人の手による設定が必要と判明したとき）
- **共通ルール**: [_rules.md](./_rules.md)

## `## 人手対応待ち`（300 字）

```markdown
## 人手対応待ち

Infisical（環境 `<ENV>`）にプレースホルダを作成しました。値が入るまで本 Issue の作業を止めます。

| 変数 | 用途 |
|:--|:--|
| `NAME` | （何に使うか 1 文） |
```

- シークレットの値・値の例は書かない
- 値の入手手順は書かない（ループの停止時にユーザーへ直接案内する）
